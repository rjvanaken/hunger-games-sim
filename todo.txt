- add speed for running for running from enemies
    which impacts exhaustion 
        which means they may need a stamina stat

DONE: add success for hunting ability to make chances of successful food pickup
    which means I may need to have 2 food types or instead just make it be a chance that it requires probability and 
    also a chance it won't need it


- build out segments


- decide if I want an exhaustion attribute


- adjust arenaknowledge when the segment they are in changes OR when they go from 1 segment to another

for key, value in segments
- for tribute in arena.tributes:
    if tribut



- ultimately may need to change it so you have to be on the same slot in order to fight and pickup food
- a source like water isn't not crossable
- or might just make water an exception. have water be in a next slot


- with the RL and the rewards, how do we get only a successful action to edit the reward if 
- we cant just say "here's the reward" if action compelted how will the RL even work
- do I need more wraparound functions?

- unless the tribute has an attribute that is like "new_rewrd" and it's edited on a successful thing
- so afterwards I just change it to that with that action bc it will only have changed if it were a success 

--- ok wait about the pickup thing - need to make it on the same spot and figur eout how to make the water different
-- NOW
-- what if, to avoid a lot of errors, actions are only available if you qualify:
-- would need conditionals to say "if with no food and not hungry (and variations)" then that is not one of the next states
-- is this possible? can I make the states change dynamically like that?
-- this would eliminate the possibility of an AI throwing an error by picking up an item that doesn't exist.
-- is there an item on your current spot and do you have room in inventory? pickup is displayed
-- are you thirsty and do you have water? drink is displayed
-- is there a tribute on your same page 